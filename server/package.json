{
  "name": "loafrtc-server",
  "version": "1.0.0",
  "private": true,
  "description": "LoafRTC signaling and static frontend server",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check turn.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "helmet": "^7.1.0",
    "ws": "^8.18.0"
  }
}
